services:
  - docker

jobs:
  include:
    - stage: Unit_test
      if: branch != master

      before_install:
        - docker pull ubuntu
        - docker build -t logickee/ubuntu-1804-zsh .

      script:
        - sh tests/run_test.sh
        - echo 'helloworld'
